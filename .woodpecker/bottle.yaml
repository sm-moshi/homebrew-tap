# homebrew-tap bottle — build ARM64 bottle on Mac via SSH, release to GitHub
when:
  - event: push
    branch: main
    path:
      - "Formula/fast-cli.rb"
  - event: manual

steps:
  - name: bottle-arm
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
<<<<<<< HEAD
    image: dhi.io/alpine-base:3.23-dev@sha256:fb5ea621f50f24533355a8c58ca68aa9b5b5cd0af080feeb7f66ba1611069212
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
      SSH_KEY:
        from_secret: mac_builder_ssh_key
    commands:
      - apk add --no-cache openssh-client bash
      - mkdir -p ~/.ssh && chmod 700 ~/.ssh
      - printf '%s\n' "$SSH_KEY" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
      - ssh-keyscan -H 10.0.10.4 >> ~/.ssh/known_hosts 2>/dev/null
      - |
        ssh smeya@10.0.10.4 "export GITHUB_TOKEN='${GITHUB_TOKEN}'; export CI_COMMIT_SHA='${CI_COMMIT_SHA}'; bash" <<'REMOTE_EOF'
        set -euo pipefail
        WORKDIR=$(mktemp -d)
        git clone --depth=50 "https://x-access-token:${GITHUB_TOKEN}@github.com/sm-moshi/tap.git" "$WORKDIR"
<<<<<<< HEAD
        cd "$WORKDIR" && git checkout "${CI_COMMIT_SHA}"
        export GITHUB_TOKEN
        bash "$WORKDIR/Scripts/bottle-arm.sh" "$WORKDIR"
        rm -rf "$WORKDIR"
<<<<<<< HEAD
        REMOTE_EOF
=======
    image: appleboy/drone-ssh
    settings:
      host: 10.0.10.4
      username: smeya
      key:
        from_secret: mac_builder_ssh_key
      port: 22
      command_timeout: 30m
      script_stop: true
      shell: /bin/bash
      envs:
        - github_token
        - ci_commit_sha
      github_token:
        from_secret: github_token
      script:
        - set -euo pipefail
        - WORKDIR=$(mktemp -d)
        - git clone --depth=50 "https://x-access-token:${GITHUB_TOKEN}@github.com/sm-moshi/homebrew-tap.git" "$WORKDIR"
        - cd "$WORKDIR" && git checkout "${CI_COMMIT_SHA}"
        - bash "$WORKDIR/Scripts/bottle-arm.sh" "$WORKDIR"
        - rm -rf "$WORKDIR"
>>>>>>> 522c7e6 (migrate: complete GHA to Woodpecker CI migration with Mac SSH builder)
=======
    image: alpine:3.21
=======
    image: dhi.io/alpine-base:3.23-dev
>>>>>>> 1649fc0 (fix: use dhi.io/alpine-base:3.23-dev for SSH pipelines)
=======
    image: dhi.io/alpine-base:3.23-dev@sha256:fb5ea621f50f24533355a8c58ca68aa9b5b5cd0af080feeb7f66ba1611069212
>>>>>>> 3c2811f (chore(deps): pin dhi.io/alpine-base docker tag to fb5ea62 (#20))
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
      SSH_KEY:
        from_secret: mac_builder_ssh_key
    commands:
      - apk add --no-cache openssh-client bash
      - mkdir -p ~/.ssh && chmod 700 ~/.ssh
      - printf '%s\n' "$SSH_KEY" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
      - ssh-keyscan -H 10.0.10.4 >> ~/.ssh/known_hosts 2>/dev/null
      - |
        ssh smeya@10.0.10.4 "export GITHUB_TOKEN='${GITHUB_TOKEN}'; export CI_COMMIT_SHA='${CI_COMMIT_SHA}'; bash" <<'REMOTE_EOF'
        set -euo pipefail
        WORKDIR=$(mktemp -d)
        git clone --depth=50 "https://x-access-token:${GITHUB_TOKEN}@github.com/sm-moshi/homebrew-tap.git" "$WORKDIR"
=======
>>>>>>> f2f3fca (chore: update repo paths for tap integration)
        cd "$WORKDIR" && git checkout "${CI_COMMIT_SHA}"
        export GITHUB_TOKEN
        bash "$WORKDIR/Scripts/bottle-arm.sh" "$WORKDIR"
        rm -rf "$WORKDIR"
        REMOTE_EOF
>>>>>>> 76c1a0d (fix: replace drone-ssh plugin with raw SSH for fish shell compat)
=======
        REMOTE_EOF
>>>>>>> c1629d2 (migrate: complete GHA to Woodpecker CI migration with Mac SSH builder)
